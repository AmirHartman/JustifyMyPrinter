{
  "name": "jmp-print-bridge",
  "version": "1.0.0",
  "private": true,
  "description": "Local LAN bridge: pulls approved print jobs from the JustifyMyPrinter site and drives the Bambu P2S over the local network.",
  "type": "commonjs",
  "main": "index.js",
  "scripts": {
    "start": "node index.js"
  },
  "engines": { "node": ">=20" },
  "dependencies": {
    "adm-zip": "0.5.16",
    "basic-ftp": "^5.0.5",
    "mqtt": "^5.10.1"
  }
}
