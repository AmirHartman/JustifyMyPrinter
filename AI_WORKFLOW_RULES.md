# AI Workflow Rules

The canonical workflow rules are in `docs/AI_WORKFLOW_RULES.md`.

This root file is intentionally only a pointer so architecture, deployment, and
feature guidance cannot drift between duplicate copies.
